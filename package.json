{
  "name": "typescript-play",
  "version": "0.0.1",
  "description": "Better TypeScript playground",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serve ./docs",
    "setup": "npm run fetch-schema",
    "clean": "rm -rf monaco-typescript && rm -rf docs/monaco-typescript",
    "get-typescript": "./scripts/get-typescript.sh",
	"deploy": "gh-pages --dist=docs",
	"build": "npx webpack --config ./config.js",
    "fetch-schema": "curl http://json.schemastore.org/tsconfig > docs/schema/tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "roblox-ts": "^0.1.11",
    "serve": "^6.5.3"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "webpack": "^4.34.0",
    "webpack-cli": "^3.3.4"
  }
}
